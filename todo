=== FIX SPLIT ARGS ===

probleme sur "cd ''" --> on recupere une simple quote en arg <-- FIXED
echo "$USER=12$USER" --> on recupere une double quote devant <-- FIXED
echo "a" --> on garde les deux quotes --> echo ou fix a split ? <-- FIXED

dans l'ensemble on print les "" ou '' avec le echo <-- FIXED


=== FIX echo ===
echo 123$USER $9USER --> manque l'espace dans le resultat

=== tab_env ===
shell->tab_env = malloc(100000) ? Pourquoi ? <-- FIXED


=== EXIT STATUS ===
voir comment recuperer les bons exit status sur les fonctions type dup, close, pipe...
